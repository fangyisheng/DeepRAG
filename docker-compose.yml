networks:
  app-network:
    driver: bridge

volumes:
  pg_data:
  minio_data:


services:
  db:
    image: postgres:latest
    container_name: deeprag-db
    env_file:
      - ./docker.env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${PG_EXPOSED_PORT}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - app-network
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: deeprag-minio
    env_file:
      - ./docker.env
    ports:
      - "${MINIO_EXPOSED_PORT}:9000"
      - "${MINIO_CONSOLE_EXPOSED_PORT}:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":${MINIO_CONSOLE_EXPOSED_PORT}"
    volumes:
      - minio_data:/data
    networks:
      - app-network
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deeprag-app
    env_file:
      - ./docker.env
    depends_on:
      - db
      - minio
    environment:
      - name=value
    ports:
      - "${DEEPRAG_APP_PORT}:8000"
    networks:
      - app-network
    restart: unless-stopped
